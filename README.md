# A-Deep-Learning-based-Next-Word-Prediction-System-built-with-TensorFlow-Keras-and-Streamlit.
This project predicts the next word in a sentence using a trained Long Short-Term Memory (LSTM) neural network.  The application takes a sequence of words as input, processes them using a tokenizer, and predicts the most probable next word learned during training.
